interface:
  display_name: "Agent"
  short_description: "Explicit Sol Advisor orchestration and verified publish gates"
  default_prompt: "Use $agent to run the mandatory Sol Advisor implementation and fresh-review route for this project."
policy:
  allow_implicit_invocation: false
