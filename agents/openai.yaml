interface:
  display_name: "Agent"
  short_description: "Локальный Sol Advisor fast path: native implementer и fresh Sol review"
  default_prompt: "Локальный Sol Advisor fast path запускается для задач по умолчанию: native Luna/Terra implementer, primary verification и fresh Sol review; $agent остаётся совместимым явным триггером."
policy:
  allow_implicit_invocation: true
