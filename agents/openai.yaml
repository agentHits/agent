interface:
  display_name: "Agent"
  short_description: "Локальный Sol Advisor fast path: native implementer и fresh Sol review"
  default_prompt: "Используйте $agent для локального Sol Advisor fast path: native Luna/Terra implementer, затем fresh Sol review; implicit invocation не используется."
policy:
  allow_implicit_invocation: false
