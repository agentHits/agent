interface:
  display_name: "Agent"
  short_description: "Verified project orchestration and publish gates"
  default_prompt: "Use $agent to orchestrate this project with verified publication gates."
policy:
  allow_implicit_invocation: false
